{% extends 'base.html.twig' %}

{% block title %} Gio Hang {% endblock %}

{% block body %}
     <div class="container">
        <div class="row mt-3 mb-4">
            <div class="col-md-4">
            {# <div class="card h-100">
            <img src="{{ asset('images/product/' ~ product.getImage()) }}" alt="{'id': product.id}" class="img-fluid" width="250px" height="155px"> #}
               <img src="{{ asset('images/product/logo2.png') }}" class="img-fluid">
            </div>
             <div class="col-md-8">
                <h1 class="mt-4 mt-md-0">Thanh toan</h1>
                 <h1>Product</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Tên</th>
                <td>{{ product.Name }}</td>
            </tr>
            <tr>
                <th>Giá</th>
                <td>{{ product.Price }}</td>
            </tr>
             <tr>
                <th>Hình Ảnh</th>
                <td><img src="{{ asset('images/product/' ~ product.getImage()) }}" width="200px" height="155px"></td>
            </tr>
            
        </tbody>
    </table>
             
               <a href="{{ path('app_product_checkout') }}" class="btn btn-secondary active"> Tính tiền </a></li>
                {# {<h2>{{ product.price }} €</h2> #}
                <hr>
    </div>  
{% endblock %}
