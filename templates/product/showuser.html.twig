{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            
            {# <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr> #}
            <tr>
                <th>Name</th>
                <td>{{ product.Name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.Price }}</td>
            </tr>
             <tr>
                <th>Mô Tả</th>
                <td>{{ product.description }}</td>
            </tr>
            <img src="{{ asset('images/product/' ~ product.getImage()) }}" width="200px" height="155px">
        </tbody>
                

    </table>

    <a class="btn btn-dark" href="{{ path('app_product_index') }}">back to list</a>
     {# <a class="btn btn-dark" href="{{ path('app_product_edit', {'id': product.id}) }}">edit</a> #}
    <br></br>
    {% if is_granted('ROLE_USER')%}
    
      <form action="{{ path('app_add_cart', {'id': product.id}) }}" method="GET">
                                        <input type="number" id="quantity" name="quantity" value="1">
                                          <br></br>
                                        <button href="{{ path('app_add_cart', {'id': product.id}) }}"
                                        class="btn btn-primary" type="submit">ADD CART</button> 
                                        <br>
    {% endif %}
    {# {{ include('product/_delete_form.html.twig') }} #}
{% endblock %}